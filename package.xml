<package format="2">
  <name>rArm</name>
  <version>0.3.0</version>
  <description>
     An automatically generated package with all the configuration and launch files for using the rArm with the MoveIt Motion Planning Framework
  </description>
  <author email="ken1999429@gmail.com">neil</author>
  <maintainer email="ken1999429@gmail.com">neil</maintainer>

  <license>BSD</license>

  <url type="website">http://moveit.ros.org/</url>
  <url type="bugtracker">https://github.com/moveit/moveit/issues</url>
  <url type="repository">https://github.com/moveit/moveit</url>

  <buildtool_depend>catkin</buildtool_depend>

  <depend>moveit_ros_move_group</depend>
  <depend>moveit_fake_controller_manager</depend>
  <depend>moveit_kinematics</depend>
  <depend>moveit_planners</depend>
  <depend>moveit_ros_visualization</depend>
  <depend>moveit_setup_assistant</depend>
  <depend>moveit_simple_controller_manager</depend>
  <depend>joint_state_publisher</depend>
  <depend>joint_state_publisher_gui</depend>
  <depend>robot_state_publisher</depend>
  <depend>rviz</depend>
  <depend>tf2_ros</depend>
  <depend>xacro</depend>
  <!-- The next 2 packages are required for the gazebo simulation.
       We don't include them by default to prevent installing gazebo and all its dependencies. -->
  <!-- <depend>joint_trajectory_controller</depend> -->
  <!-- <depend>gazebo_ros_control</depend> -->
  <!-- This package is referenced in the warehouse launch files, but does not build out of the box at the moment. Commented the dependency until this works. -->
  <!-- <depend>warehouse_ros_mongo</depend> -->
  <depend>roscpp</depend>
  <depend>rospy</depend>
  <depend>sensor_msgs</depend>
  <depend>serial</depend>
  <depend>control_msgs</depend>
  <depend>trajectory_msgs</depend>
  <depend>actionlib</depend>
  <depend>actionlib_msgs</depend>

</package>
